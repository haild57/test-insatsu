version: 0.2

phases:
  build:
    commands:
      - echo Building SpringBoot App...
      - cd app 
      - chmod +x mvnw
      - ./mvnw clean install

artifacts:
  files:
    - app/target/sample-springboot.war
    - appspec.yml
    - scripts/**/*
  discard-paths: no